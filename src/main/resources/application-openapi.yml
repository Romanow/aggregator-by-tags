application:
  apis:
    store:
      local: openapi/store.yml
      external: file:///opt/openapi/store/store.yml
      prefix: /store
    orders:
      local: openapi/orders.yml
      prefix: /orders
    warehouse:
      local: openapi/warehouse.yml
      prefix: /warehouse
    warranty:
      local: openapi/warranty.yml
      prefix: /warranty
